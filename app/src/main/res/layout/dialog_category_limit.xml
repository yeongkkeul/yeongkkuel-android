<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="330dp"
    android:layout_height="190dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@drawable/bg_category_limit_dialog"
    android:padding="20dp"
    android:elevation="10dp"
    android:layout_gravity="center">

    <!-- 제목 -->
    <TextView
        android:id="@+id/tv_limit_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="카테고리 6개가 꽉 찼어요!"
        android:textColor="@color/main1"
        style="@style/title_semib"
        android:textStyle="bold"
        android:layout_marginTop="15dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- 설명 -->
    <TextView
        android:id="@+id/tv_limit_message"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="기존 카테고리를 수정하거나 삭제할 수 있어요."
        android:textColor="@color/black"
        style="@style/body_medium"
        android:layout_marginTop="15dp"
        app:layout_constraintTop_toBottomOf="@id/tv_limit_title"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- 확인 버튼 -->
    <TextView
        android:id="@+id/tv_reward"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="확인"
        android:gravity="center"
        android:background="@drawable/bg_category_dialog_btn"
        android:textColor="@color/white"
        android:clickable="true"
        android:focusable="true"
        style="@style/body_medium"
        android:layout_marginTop="20dp"
        app:layout_constraintTop_toBottomOf="@id/tv_limit_message"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>



</androidx.constraintlayout.widget.ConstraintLayout>